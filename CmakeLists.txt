cmake_minimum_required(VERSION 3.10)
project(CPlusPlus-Standard VERSION 1.0 LANGUAGES CXX C)

message(STATUS "$ENV{HOME}")

if(UNIX)
    message(STATUS "[Linux env usage]: cd cmakebuild && cmake -DCMAKE_BUILD_TYPE=Debug ../ ")
elseif(WIN32)
    message(STATUS "[Windows env MinGW usage]: cd cmakebuild && cmake -G \"MinGW Makefiles\" ../ ")
    set_property(GLOBAL PROPERTY USE_FOLDERS On)
elseif(APPLE)
    message(STATUS "This operating system is APPLE.")
endif()

# 添加各个C++标准版本的子项目
add_subdirectory(CPlusPlus11)
add_subdirectory(CPlusPlus14)
add_subdirectory(CPlusPlus17)
add_subdirectory(CPlusPlus20)
add_subdirectory(CPlusPlus23)